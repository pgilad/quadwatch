[tools]
go = "1.26.5"

[tasks.build]
description = "Build the quadwatch binary"
run = "go build -o quadwatch ."

[tasks.install]
description = "Install the quadwatch binary to ~/.local/bin"
depends = ["build"]
run = "mkdir -p ~/.local/bin && cp quadwatch ~/.local/bin/quadwatch"
